{
  "name": "@streamz/worker-publisher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@streamz/shared": "workspace:*",
    "@streamz/db": "workspace:*",
    "bullmq": "^5.30.0",
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
